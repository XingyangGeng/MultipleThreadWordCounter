# MultipleThreadWordCounter
program using Java concurrency mechanisms (i.e., Threads / ServiceExecutors / latch objects).
use multiple threads to couter the word in large numbers of seperated txt files.
